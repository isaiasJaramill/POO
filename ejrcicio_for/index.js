// ejercicio ingrese cinco numeros y verifique cual es el mayor

function bumeroMayor(){
    let arraynum = [];
    let numMayor = 0;
}
    for( let i = 0; 1 < 5; i++){
         let num = parseInt(prompt("ingrese un numero para detertminar el mayot "));
    arraynum.push(num);
    if(numMayor > num){
        numMayor = num;
     
    }else{
        
    }
    }